def strings_xor(s, t):
    res = []
    for i in range(len(s)):
        if s[i] == t[i]:
            res.append(0)
        else:
            res.append(1)
    
    r= ''.join(str(x) for x in res)

    return r
